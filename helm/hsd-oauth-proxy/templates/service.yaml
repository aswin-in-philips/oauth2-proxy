apiVersion: v1
kind: Service
metadata:
  name: {{ include "hsd-oauth-proxy.fullname" . }}
  labels:
    {{- include "hsd-oauth-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hsd-oauth-proxy.selectorLabels" . | nindent 4 }}
